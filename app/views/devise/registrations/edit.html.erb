<div class="container">
  <div class="row">
    <div class="col-sm-6 col-sm-offset-3">

      <h2><%= resource.username.to_s.humanize %>さんのプロフィール編集</h2><hr>

      <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <table class=devise_table>

          <tr>
            <th><%= f.label :username %></th>
            <td><%= f.text_field :username, autofocus: true, autocomplete: "username" %><td>
          </tr>

          <tr>
            <th><%= f.label :email %></th>
            <td><%= f.email_field :email, autocomplete: "email" %></td>
          </tr>

          <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
            <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
          <% end %>

          <tr>
            <th><%= f.label :password %> <i>(パスワードを変更しない場合は空白のままにします。)</i></th>
            <td><%= f.password_field :password, autocomplete: "new-password" %>
            <% if @minimum_password_length %><br>
              <em><%= @minimum_password_length %> 文字以上で入力してください</em>
            <% end %></td>
          </tr>

          <tr>
            <th><%= f.label :password_confirmation %></th>
            <td><%= f.password_field :password_confirmation, autocomplete: "new-password" %></td>
          </tr>

          <tr>
            <th><%= f.label :current_password %> <i>(変更をするためには現在のパスワードを入力してください)</i></th>
            <td><%= f.password_field :current_password, autocomplete: "current-password" %></td>
          </tr>

        </table><hr>

        <div class="actions">
          <%= f.submit "更新する", class: 'devise-btn' %>
        </div><hr>
      <% end %>

      <h3>アカウントの削除</h3>

      <p>以下のボタンを押せばアカウントが削除されます <%= button_to "アカウントを削除する", registration_path(resource_name), data: { confirm: "本当にアカウントを削除しますか?" }, method: :delete, class: 'devise-btn' %></p>

    </div>
  </div>
</div>
