<p>こんにちは <%= @resource.username %>さん!</p>

<p>あなたのパスワードを変更するためのリクエストが行われました。以下のリンクから変更を行うことができます。</p>

<p><%= link_to 'Change my password', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>リクエストをしていない場合、このメールを無視してください。</p>
<p>上記のリンクにアクセスし、新しいパスワードを作成するまで、パスワードは変更されません。</p>
