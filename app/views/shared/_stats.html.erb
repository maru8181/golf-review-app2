<div class="stats">
  <a href="<%= following_member_path(@user) %>", class='mypage_link' >
    <strong id="following" class="stat">
      <%= @user.following.count %>
    </strong>
    フォロー
  </a>
  <a href="<%= followers_member_path(@user) %>", class='mypage_link' >
    <strong id="followers" class="stat">
      <%= @user.followers.count %>
    </strong>
    フォロワー
  </a>
</div>
